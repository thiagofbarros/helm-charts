{{- if .Values.deployment.hpa.enabled  }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "vaultwarden.fullname" . }}
  labels:
    {{- include "vaultwarden.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "vaultwarden.fullname" . }}
  minReplicas: {{ .Values.deployment.hpa.minReplicas }}
  maxReplicas: {{ .Values.deployment.hpa.maxReplicas }}
  metrics:
    {{- if .Values.deployment.hpa.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.deployment.hpa.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.deployment.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.deployment.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}